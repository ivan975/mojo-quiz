import React from 'react';

const Blogs = () => {
    // const { id, name, logo } = quiz;
    return (
        <div>
            {/* <h2>blogs:{id}</h2> */}
        </div>
    );
};

export default Blogs;